export const AUDIT_DETAIL_DIALOG_OPTIONS = {
  width: 'min(1200px, 96vw)',
  height: 'min(860px, 92vh)',
  maxWidth: '96vw',
  maxHeight: '92vh',
  panelClass: 'audit-dialog-panel',
  autoFocus: false,
} as const;

export const CREATE_AUDIT_DIALOG_OPTIONS = {
  width: 'min(1100px, 96vw)',
  height: 'min(760px, 92vh)',
  maxWidth: '96vw',
  maxHeight: '92vh',
  panelClass: 'audit-dialog-panel',
  autoFocus: false,
} as const;

export const ADD_AUDIT_LINE_DIALOG_OPTIONS = {
  width: 'min(640px, 96vw)',
  maxWidth: '96vw',
  maxHeight: '88vh',
  autoFocus: false,
} as const;
